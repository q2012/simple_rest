variables:
    PACKAGE_PATH: /go/src/app

stages:
    - build
    - deploy

build:
    stage: build
    image: docker:17
    services:
      - docker:dind
    script:
        - docker build -t simple-rest .
