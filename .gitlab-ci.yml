variables:
    PACKAGE_PATH: /go/src/app

stages:
    - build
    - deploy

build:
    stage: build
    image: docker:latest
    services:
      - docker:dind
    script:
        - export DOCKER_HOST="tcp://localhost:2375"
        - docker build -f Dockerfile -t simple-rest .
