variables:
    PACKAGE_PATH: /go/src/app

stages:
    - build
    - deploy

build:
    stage: build
    image: docker:latest
    services:
      - docker:dind
    script:
        - docker build -f Dockerfile -t simple-rest .
